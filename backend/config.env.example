# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/job-master

# JWT Secret (REQUIRED - Use a strong, random secret)
JWT_SECRET=your-super-secret-jwt-key-here-change-in-production

# Admin Codes (comma-separated, change in production)
ADMIN_CODES=ADMIN2024,FACULTY2024,MASTER2024

# Email Configuration (Gmail)
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password

# Frontend URL
FRONTEND_URL=http://localhost:3000

# Optional: Use different email service
# For Gmail, you need to:
# 1. Enable 2-factor authentication
# 2. Generate an App Password
# 3. Use the App Password instead of your regular password

# Alternative email services:
# - SendGrid: SENDGRID_API_KEY=your-api-key
# - AWS SES: AWS_ACCESS_KEY_ID=your-key, AWS_SECRET_ACCESS_KEY=your-secret
# - Mailgun: MAILGUN_API_KEY=your-api-key, MAILGUN_DOMAIN=your-domain
